<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.che_ti_bleEX">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_chetible"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_chetible_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SettingActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".Showtimetable" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".ChatActivity" /> <!-- 위에서 부터 순서대로 시작하는 activity설정, 앱 시작할 때 무조건 세로로 고정해서 시작, splash acitivity에 사용되는 화면 불러오는 것 등이다. -->
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>